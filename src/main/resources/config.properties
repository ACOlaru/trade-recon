# price tolerance expressed as absolute difference
price.tolerance=0.0001

# whether string comparisons (symbols) are case-insensitive
compare.symbols.caseInsensitive=true

# quantity tolerance (absolute)
quantity.tolerance=0.0

# output format for reports: CSV or JSON
report.format=CSV